. base instruction

  Setup your .git/config
  git config --add remote.dguibert.fetch refs/insns/*:refs/insns/*
  git config --add remote.dguibert.fetch +refs/heads/dg/*:refs/heads/dg/*

  use gh to get branch form a PR
  gh pr checkout XXXX

base spack/develop

. merge feature/simpler-compiler-bootstrap

  not needed for nixpack-parts

merge dg/restore-config-locks

. example d integration de PR
. exec   gh pr checkout renjithravindrankannath:rocm-6.0.0-rel-br
. merge rocm-6.0.0-rel-br

merge dg/modules

merge dg/misc

merge dg/fckit-with-nvhpc

merge dg/oci-insecure-registry

merge dg/update-minimap2

merge dg/update-racon

merge dg/iqbal-lab-pkgs

  Developpers don't care about having their tools in SPACK, see
  https://github.com/iqbal-lab-org/cylon/issues/21

  But we do.

merge dg/bootstrap-readonly

merge dg/hcoll-requires-to-depends
